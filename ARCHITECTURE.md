# ARCHITECTURE

## 1. 文档目标
本文件用于说明当前项目的系统结构、核心模块、主链路、数据流、部署形态，以及后续商业化改造时的拆分方向。

## 2. 当前架构结论
### 2.1 当前形态
- 当前项目本质上是一个以 `Streamlit` 为入口的单体应用。
- 核心能力集中在 Python 侧，页面、业务编排、部分状态管理、媒体处理入口都聚合在一个项目中。
- 项目同时连接了多个外围组件：
  - 浏览器扩展（Chrome / Edge / Firefox）
  - bridge API
  - 本地转写工具
  - Render 部署环境

### 2.2 当前优点
- 开发快，验证链路效率高。
- 媒体处理、字幕获取、转写、总结、事实核查能力已经形成闭环。
- 插件与本地工具双兜底思路已经建立。

### 2.3 当前局限
- `app.py` 仍较大，页面层与业务编排耦合偏高。
- 运行时状态和本地文件状态较多。
- 更适合单人开发和功能验证，不适合直接承载多用户商业化。

## 3. 系统组成
### 3.1 Web 主站
- 技术形态：`Streamlit`
- 主要职责：
  - 接收用户输入
  - 展示总结结果
  - 展示事实核查结果
  - 管理订阅、任务、内容资产库、设置与诊断
  - 调度插件 bridge、本地工具和核心处理逻辑

### 3.2 核心处理层
- 当前主要由 Python 模块组成，核心文件包括：
  - `app.py`
  - `core_logic.py`
  - `bridge_api.py`
  - `task_runner.py`
  - `local_fetch_node.py`

主要职责：
- 视频信息提取
- 字幕获取
- 音频抽取
- Whisper 转写
- 文本总结
- 事实核查
- 频道更新抓取

### 3.3 浏览器扩展
- 目录：`chrome_extension_mvp/`
- 主要职责：
  - 在页面侧提取可用文本
  - 在无法直接在线稳定抓取时提供主路径兜底
  - 调用 bridge API 上传 payload
  - 从统一源码产出 `Chrome / Edge / Firefox` 三个浏览器版本

### 3.4 bridge API
- 主要文件：`bridge_api.py`
- 主要职责：
  - 接收插件或本地工具上传的 payload
  - 兼容旧版和新版 bridge contract
  - 让主站按 `payload_id` 拉取数据

### 3.5 本地转写工具
- 目录：`local_cli_mvp/`
- 主要职责：
  - 在无文本时获取音频
  - 依赖 `yt-dlp + ffmpeg + whisper`
  - 将转写结果回传主站或走本地辅助链路

### 3.6 共享契约
- 目录：`shared/`
- 典型文件：`transcript_contracts.ts`
- 主要职责：
  - 统一扩展、本地工具、bridge、主站之间的数据结构定义

## 4. 当前页面结构
当前一级导航已重构为 5 个页面方向：
- 处理中心
- 任务中心
- 订阅自动化
- 内容资产库
- 设置与诊断

### 4.1 处理中心
包含：
- 单视频处理
- 手动粘贴字幕总结
- 文档总结

### 4.2 任务中心
包含：
- 后台任务状态
- 最近执行
- 运行日志

### 4.3 订阅自动化
包含：
- 频道订阅管理
- 最新动态
- 定时任务相关能力

### 4.4 内容资产库
包含：
- 历史记录
- 搜索
- 结构化结果复用

### 4.5 设置与诊断
包含：
- 运行环境信息
- 模型与代理配置
- 诊断信息

## 5. 当前核心数据流
### 5.1 普通视频总结链路
1. 用户输入视频链接。
2. 主站尝试直接获取字幕或文本。
3. 若获取成功，交给大模型总结。
4. 若获取失败，则尝试插件或本地工具兜底。
5. 总结结果写入历史记录，并在主站展示。

### 5.2 插件桥接链路
1. 插件从视频页面提取文本。
2. 文本通过 bridge API 上传。
3. 主站根据 `payload_id` 拉取 payload。
4. 主站注入文本并触发自动总结。

### 5.3 本地工具兜底链路
1. 主站或用户触发本地工具。
2. 本地工具使用 `yt-dlp` 抓取媒体。
3. 使用 `ffmpeg` 处理音频。
4. 使用 Whisper 转写。
5. 将文本送回主站总结。

### 5.4 订阅自动化链路
1. 用户维护订阅频道。
2. 主站检查频道最近更新。
3. 过滤出需要关注的视频。
4. 用户手动或定时触发后续总结。

## 6. 当前状态存储
### 6.1 本地文件
当前仍存在本地文件状态：
- `history.json`
- `subscriptions.json`
- 部分本地配置文件

### 6.2 会话状态
- 使用 `Streamlit session_state` 保存页面运行时状态、任务视图状态、桥接状态等。

### 6.3 当前问题
- 这种方式适合单用户或本地工具场景。
- 不适合真正的多用户、权限隔离、计费和审计。

## 7. 当前部署形态
### 7.1 主站
- 部署在 Render

### 7.2 bridge 服务
- 单独作为 bridge API 使用

### 7.3 本地工具
- 运行在用户本机

### 7.4 插件
- 运行在用户浏览器侧

## 8. 当前关键设计原则
### 8.1 主路径优先
- 优先用插件提取文本
- 避免纯在线抓取字幕导致平台风控问题

### 8.2 本地兜底
- 当无法直接获取文本时，转到本地工具处理音频

### 8.3 保持最小修复
- 近期修复都遵循“最小定点改动”
- 避免大规模重构造成主链路回归

## 9. 当前已确认的重要实现判断
### 9.1 事实核查
- 已收紧触发条件
- 明显测试文本、链路验证文本、产品演示文本默认不做新闻事实核查

### 9.2 订阅更新
- 已修复“不是最新”的问题
- 当前策略已改为按发布时间稳定排序
- 对部分频道取消错误的仅 `streams` 限制

### 9.3 内容资产库
- 已优化空状态
- 已增加历史命中数展示

## 10. 当前模块边界建议
### 10.1 保持在 `app.py` 的内容
- 页面装配
- 交互逻辑
- 轻量的 UI 状态处理

### 10.2 应继续从 `app.py` 抽离的内容
- 更细的页面渲染函数
- 更纯粹的业务逻辑
- 更独立的任务调度逻辑
- 更独立的资产库逻辑

### 10.3 应继续沉淀在 `core_logic.py` 或独立模块的内容
- 字幕获取
- 频道视频抓取
- 总结
- 事实核查
- 音频处理
- 外部接口调用

## 11. 后续商业化拆分方向
当前推荐未来拆成两层：

### 11.1 业务系统层
建议使用正式 Web + API 架构承接：
- 用户注册登录
- 历史记录
- 订阅与自动化
- 配额与套餐
- 支付与账单
- 审计日志

### 11.2 AI 处理能力层
保留并演进现有 Python 能力：
- 媒体抓取
- 字幕提取
- 音频处理
- Whisper 转写
- 总结
- 事实核查

## 12. 推荐未来目标架构
### 12.1 前端
- `Next.js`

### 12.2 业务后端
- `NestJS` 或 `Next.js Route Handlers`

### 12.3 AI 处理服务
- Python 独立服务

### 12.4 数据层
- `PostgreSQL`
- `Redis`
- 对象存储

### 12.5 认证与支付
- 认证：`Supabase Auth` 或自建认证
- 支付：`Stripe`（海外优先）

## 13. 当前架构风险
- 单体结构继续膨胀会降低维护性。
- 本地文件状态不适合商业化。
- 插件、本地工具、主站之间尚未统一用户鉴权。
- 过多核心逻辑集中在单体页面应用中，不利于长期演进。

## 14. 架构演进建议
### 第一阶段
- 保持当前主站可用
- 继续做最小修复
- 补齐文档和运行手册

### 第二阶段
- 把核心处理能力逐步 API 化
- 逐步把状态和数据迁移到数据库

### 第三阶段
- 引入用户系统
- 引入权限与配额
- 引入支付与账单
- 形成真正的 SaaS 架构

## 15. 推荐阅读顺序
若是第一次接手本项目，建议按以下顺序阅读：
1. `README.md`
2. `AGENT_CONTEXT.md`
3. `ARCHITECTURE.md`
4. `RUNBOOK.md`
5. `MIGRATION_NOTES.md`
6. `COMMERCIALIZATION_PLAN.md`
