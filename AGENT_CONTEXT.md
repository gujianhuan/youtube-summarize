# AGENT_CONTEXT

## 1. 项目定位
- 项目名称：`YouTubeSummarizer`
- 当前形态：以 `Streamlit` 为主的单体应用，集成视频处理、文本总结、文档总结、订阅自动化、任务中心、内容资产库、设置与诊断。
- 当前阶段：功能验证已基本跑通，正在向“可持续交付 + 可商业化重构”过渡。

## 2. 当前主链路
### 2.1 视频总结主链路
1. 用户输入 YouTube / Bilibili 链接。
2. 系统优先尝试直接获取字幕或文本。
3. 无法稳定获取文本时，走浏览器扩展（Chrome / Edge / Firefox）提取或本地工具音频转写兜底。
4. 将文本交给大模型做结构化总结。
5. 结果进入页面展示，并写入历史记录。

### 2.2 插件桥接主链路
1. 浏览器扩展提取页面文本。
2. 通过 bridge API 上传 payload。
3. 主站通过 `ext_payload_id` 读取 bridge payload。
4. 主站自动注入文本并触发总结。

### 2.3 本地工具兜底链路
1. 无法直接拿到字幕时，本地工具拉取音频。
2. 使用 `yt-dlp + ffmpeg + whisper` 做转写。
3. 将转写文本回传主站进行总结。

## 3. 最近已确认完成的事项
- 已验证浏览器扩展 -> bridge -> 主站总结这条链路可用，当前扩展按 `Chrome / Edge / Firefox` 统一维护。
- 已收紧事实核查触发条件，测试文本、链路验证文本、产品演示文本默认不再误触发新闻事实核查。
- 已优化事实核查展示：
  - 无事实核查内容时不强行展示右侧区域。
  - “来源链接”文案改为更准确的“核查来源”。
- 已修复“检查所有订阅更新”命中旧内容的问题：
  - 取消部分频道错误的仅 `streams` 限制。
  - 统一按发布时间排序，而不是按抓取顺序直接截取。
- 已优化内容资产库空状态与筛选栏：
  - 空状态下隐藏“清空历史”按钮。
  - 增加历史总数与命中数提示。
- 上述修复已推送到远端 `main`。

## 4. 最近关键提交
- 最近一次明确记录并已推送的业务修复提交：
  - `6c890dca15a4bf713f727ad3cb63487d774c717b`
  - 提交信息：`Fix subscription latest updates and library UI`

## 5. 当前代码层判断
### 5.1 优点
- 核心链路已经具备可演示价值。
- 视频、文本、文档、订阅自动化、任务中心这些能力已聚合到一个项目中。
- Python 侧在媒体处理、字幕抓取、转写、总结方面已经积累了较多可复用逻辑。

### 5.2 当前结构问题
- `app.py` 仍然偏大，顶层耦合较高。
- 仍存在较多本地文件状态：
  - `history.json`
  - `subscriptions.json`
- 当前架构更偏单用户/本地工具形态，不适合直接承接真正的多用户商业化。

## 6. 当前产品判断
- 这个项目已经过了“能不能做”的阶段，当前问题是“要不要按产品形态重构”。
- 若要商业化，不能继续长期依赖：
  - 本地 JSON 存储历史和订阅
  - `session_state` 承担多用户业务状态
  - 单体 Streamlit 承担用户系统、权限、计费、历史、任务全部职责

## 7. 商业化方向判断
- 推荐方向：拆成“业务系统 + AI 处理服务”两层。
- 业务系统负责：
  - 用户注册登录
  - 用户隔离
  - 历史记录
  - 订阅与自动化规则
  - 套餐、额度、支付、审计
- AI 处理服务负责：
  - 字幕抓取
  - 音频抽取
  - 本地或云端转写
  - 总结
  - 事实核查

## 8. 已知风险
- 目前仍不是正式的多用户 SaaS 架构。
- 历史记录、订阅、任务记录仍需逐步数据库化。
- 插件、本地工具、主站之间的用户身份尚未统一鉴权。
- 商业化前还需要补齐：
  - 用户系统
  - 权限边界
  - 配额/套餐
  - 支付
  - 审计日志
  - 数据隔离与安全策略

## 9. 新电脑迁移建议
- 可以直接复制整个 `D:\Program Files\Trae` 目录到新电脑，优先保证路径一致。
- 这不是最干净的迁移方案，但对当前阶段是最省事的。
- 复制后优先验证：
  - `git status`
  - `python --version`
  - `ffmpeg -version`
  - `streamlit run app.py`
- 若新电脑报错，再按环境问题逐项修复。

## 10. 当前硬件判断
- 新电脑配置：`i7-6700HQ / 8GB RAM / GTX 950M / 512GB SSD`
- 结论：
  - 跑页面、云端总结、订阅管理、历史记录：可以。
  - 跑本地 Whisper 长视频转写：能跑，但会慢。
  - 跑重度本地 AI、多任务并发：不建议。

## 11. 推荐下一步
### 11.1 若继续当前版本交付
- 完成新电脑迁移与环境自检。
- 确认浏览器扩展、本地工具、bridge、订阅更新都能在新电脑工作。

### 11.2 若进入商业化改造
- 输出正式 SaaS 架构文档。
- 设计数据库表结构。
- 设计用户系统、历史记录、配额与支付方案。
- 规划插件/本地工具统一鉴权方案。

## 12. 后续继续协作时的推荐输入
换电脑或新会话后，建议直接提供以下信息：
1. 当前提交哈希。
2. `AGENT_CONTEXT.md` 内容。
3. 当前报错或当前目标。
4. 是否已成功启动 `streamlit run app.py`。
